# Public VAPID key generated once by the push backend. Never put the private key here.
VITE_VAPID_PUBLIC_KEY=

# HTTPS endpoint that stores the authenticated user's PushSubscription.
VITE_PUSH_SUBSCRIBE_URL=
